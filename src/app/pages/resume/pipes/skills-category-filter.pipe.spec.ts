import { SkillsCategoryFilterPipe } from './skills-category-filter.pipe';

describe('SkillsCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillsCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
